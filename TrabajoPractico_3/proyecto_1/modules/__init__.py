"""
Inicialización del paquete 'modules'.

Este archivo marca el directorio 'modules' como un paquete de Python,
permitiendo que sus submódulos (como 'dominio', 'gestor_usuarios', etc.)
puedan ser importados utilizando rutas relativas (ej. `from . import dominio`).

Actualmente no contiene lógica de inicialización específica, pero podría
usarse para configurar variables globales del paquete o realizar otras
acciones de configuración al cargar el paquete.
"""